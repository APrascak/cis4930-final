language: node_js
node_js:
  - "stable"
cache:
  directories:
    - node_modules
script:
  - cd frontend
  - npm install
  - npm start & wait-on http://localhost:8080
  - cypress run
on:
  branch: travis